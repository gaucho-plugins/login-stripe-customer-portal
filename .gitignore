
.DS_Store

/dev files

# wp-env local development (runtime — not committed)
.wp-env/
wordpress/
tests-wordpress/
.wp-env.override.json

# Playwright MCP scratch — test-only, never ship
.playwright-mcp/
